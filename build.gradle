apply plugin: 'java'

sourceSets {
    main.java.srcDir "src/main"
}

repositories {
    mavenCentral()
}

dependencies {
    
}

jar {
    from configurations.compile.collect {
        zipTree it
    }
    manifest.attributes "Main-Class": "edu.up.numeds.NumericalMethods"
}
